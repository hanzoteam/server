openapi: "3.0.0"
info:
  title: Test API
  version: "1.0.0"
paths:
  /v1/team/users/ids:
    post:
      summary: Get users by IDs
      responses:
        "200":
          description: OK
  /v1/team/groups/{group_id}/teams/{syncable_id}/link:
    post:
      summary: Link a group syncable
      responses:
        "200":
          description: OK
